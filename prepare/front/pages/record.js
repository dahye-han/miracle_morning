import React from 'react';

import AppLayout from '../components/AppLayout';

const record = () => {
    return (
        <AppLayout>
            record
        </AppLayout>
    );
};

export default record;